#ifndef PROBA__H
#define PROBA__H

#include <stdlib.h>
#include <stdio.h>
#include <stdint.h>

float combinasion( uint8_t x, uint8_t n);
float arrangement( uint8_t x, uint8_t n);
uint32_t fact (uint8_t x);

#endif
